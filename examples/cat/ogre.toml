[project]
name = "cat"
version = "0.1.0"
description = "Cat program: reads input and echoes it back"
entry = "src/main.bf"

[build]
include = ["src/"]

[[tests]]
name = "Basic"
file = "tests/basic.json"
