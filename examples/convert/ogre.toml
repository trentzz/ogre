[project]
name = "convert"
version = "0.1.0"
description = "A brainfuck text conversion tool supporting --upper, --lower, --reverse, --ascii-to-decimal, --decimal-to-ascii"
entry = "src/main.bf"

[build]
include = ["src/", "lib/"]

[[tests]]
name = "Basic"
file = "tests/basic.json"
